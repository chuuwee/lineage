from version import VERSION

CATEGORY_ID_BY_CATEGORY = {
  'TARGET': 13,
  'LOCALE': 11,
  'EVENT': 12,
}

# TODO(ISSUE-12): Dynamically update version so it doesn't mistakenly get stale.
DKP_SCRIPT_VERSION_TAG = 'Generated by DKP script v{}.\nhttps://github.com/chuuwee/lineage'.format(VERSION)